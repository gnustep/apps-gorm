{
  NSApplication = {
    Super = NSResponder;
    Outlets = (
      delegate
    );
  };
  NSButton = {
    Super = NSControl;
  };
  NSControl = {
    Actions = (
      "takeDoubleValueFrom:",
      "takeFloatValueFrom:",
      "takeIntValueFrom:",
      "takeObjectValueFrom:",
      "takeStringValueFrom:"
    );
    Outlets = (
      target
    );
    Super = NSView;
  };
  NSSlider = {
    Super = NSControl;
  };
  NSTextField = {
    Outlets = (
      delegate
    );
    Super = NSControl;
  };
  NSResponder = {
    Super = NSObject;
  };
  NSView = {
    Super = NSResponder;
  };
  NSWindow = {
    Outlets = (
      delegate
    );
    Super = NSResponder;
  };
}
